Home work 13.02.2026

Elektronen als Teilchen und Welle Formel zusammenfassung.


Lichtgeschwindigkeit
#align(center)[
  $c = lambda * f$
]

\
\
\

c = Lichtgeschwindigkeit = $3 * 10^8 m/s$\
lambda = Wellenlänge = $m$\
f = Frequenz = $H_Z$ \


Impuls Formel:
#align(center)[
  $P_P = (h * f) / c$ $= h / f$
]
$P_p$ = Impuls des Photons \



\
\
\
\


let a = "Planksche Konstante"
let b = "Wellenlänge"


Louis de Broglie wendet die Theorie für Photonen auf jegliches Material an.
#align(center)[
  Energie = Geschwindigkeit $*$ Masse = $h / lambda$
]

$h$ = Planksche Konstante = $6.626 * 10^(-34) J * s$ \
$lambda$ = Wellenlänge

Mit dem können wir die Folgende Formel herleiten:

#align(center)[
  $p = m * v = h / lambda$
]
$lambda$ = Materiewellenlänge \


\
\
Kinetische Energie $E$ durch den Impuls ausgedrückt:
#align(center)[
  $E_k = (m * v^2) / 2 = p^2 / (2 * m)$
]






Beispiel Rechnung:
Wellenlänge einer Person welche $80$ kg schwer ist und mit $2$ $(k m)/h$ läuft:


#pagebreak()

Energie Erhaltungssatz:

#align(center)[
  $E_1 = E_2$
  \
  \
  \
  $u * abs(Q) = (m * v^2) / 2$ $= p^2 / (2 * m_e)$

  \
  \
  \
  $lambda = h / (sqrt(2 * m_e * u * e))$
  \
  \
  \
  $lambda ≈ 1 /(sqrt(u))$

]

$u$ = Spannung \
$abs(Q)$ = Ladung \
$m$ = Masse \
$v$ = Geschwindigkeit \
$p$ = Impuls \
$m_e$ = Masse des Elektrons \


Bragg-Bedingung
#align(center)[
  $n * lambda = 2 * d * sin(theta)$
]

$n$ = Beugungsordnung \
$lambda$ = Wellenlänge \
$d$ = Gitterkonstante \
$theta$ = Winkel \

Konstruktive Interferenz

#align(center)[
  $v_n = sin^-1 (n * lambda) / (2 * d)$
]
